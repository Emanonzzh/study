# Agent 项目改进路线（rs-ai-assistant）

> 原则：**只改面试官会问的**。项目已够投初级岗，改进收益递减——所以先做 P0，P1 边投边做，P2 第 4 周，P3 有余力。
> 对应岗位：Agent 开发（初级）· AI 应用部署运维（初级）

---

## P0 · 可讲性 + 工程基本盘（今晚/明天，2-3 小时，不加功能）

| # | 问题（实际位置） | 怎么改 | 面试官为什么会问 |
|---|---|---|---|
| 1 | **自己讲不清** 3 工具 / 评测数字 / 架构 | 读 `day21.py` + 用 `docs/项目口述稿.md` 练到不看稿能说 | 唯一会**直接淘汰**的问题 |
| 2 | `day21.py:10` `api_key` 默认 `"YOUR_DASHSCOPE_API_KEY"` | 启动就检查，没配就报错；补 `.env.example` + `requirements.txt` | "你怎么管理密钥/依赖" |
| 3 | `query_regulation`（`:35-41`）只返回条款正文 | 返回时带**条款号/来源前缀** | RAG 必问"怎么溯源" |
| 4 | `query_dataset`（`:46-64`）无 try/except、无 LIMIT | 加异常处理 + LIMIT，与 risk 工具风格统一 | "外部调用出错怎么办" |
| 5 | 魔法数字 `k=4` / `chunk_size=400` / 阈值 10·40·30·3 | 抽到文件顶部常量区，注释"由评测得出" | "这些参数怎么定的" |
| 6 | README 缺**演示截图** + **评测结果表** | 截图 + 83%→100% 表格贴进 README | 让 HR 3 秒看懂证据 |

## P1 · Agent 深度（1-2 周，边投边做）

| # | 改进 | 面试对应问题 |
|---|---|---|
| 7 | 工具调用**日志留痕**（时间/工具名/入参/耗时/返回长度） | "Agent 走错一步怎么排查"（可观测性） |
| 8 | **多轮对话 + 上下文控制**（`st.session_state` + 滑动窗口截断） | "上下文超长怎么办"（必问） |
| 9 | **失败退出与超时**：`max_iterations`、LLM 超时/重试、工具异常统一返回 | "Agent 死循环/失败了怎么办"（必问） |
| 10 | **按意图切换 prompt**：现在所有问题都强塞"分节报告+免责声明" | "prompt 怎么设计/怎么控输出" |
| 11 | **评测升级**：12 → 30-50 题 + 回答质量维度；结果存 `eval_result.md` | 补上"12 题样本小"的短板 |

## P2 · 运维岗方向（第 4 周）

| # | 改进 |
|---|---|
| 12 | `/health` 健康检查 + 简单指标（放 `api_rag.py`） |
| 13 | `docker compose`（app + 数据卷）+ `.dockerignore` |
| 14 | **GitHub Actions**：push → pytest → build → 部署（运维岗核心卖点） |
| 15 | 日志轮转 + Uptime Kuma 探活；把 `deploy.sh` 写进 README |

## P3 · 差异化（有余力）

| # | 改进 |
|---|---|
| 16 | **HITL 人工审批**（V2-D）：高风险建议需人工确认 → 差异化亮点 |
| 17 | `deformation_analysis` 集成为**第 4 工具**（V2-B 收尾） |
| 18 | `pytest` 覆盖核心函数（risk 打分 / recent_rate / 切片） |

---

## ⛔ 别做（时间陷阱）

多 Agent 编排、K8s、React 前端、模型微调、换 Milvus —— 初级岗不问，投递期不值得。

---

## 明天开工顺序（建议）

1. **P0-1 讲清项目**（最重要，30 分钟）：打开 `day21.py` 读 32-137 行 → 合上，口述 3 工具 + 数据流
2. **P0-2~6** 让我改（20 分钟），你 review 一遍
3. 继续投递 20-30 家 + 手写 3 题

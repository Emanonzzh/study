# 销售经营分析 Agent · 项目框架

> ⚠️ **编号提醒**：简历上这是**项目一**；但 `docs/求职/` 里其他文档因为历史原因称它"项目二"。本文一律用项目名，不提编号。
> 状态标注：✅ 已实现并验证 ｜ 🟡 部分实现 ｜ ⬜ 未开始
> 最后核对：2026-09-14（按实际文件核对，非记忆）

---

## 一、整体框架（现状标注）

```
┌─────────────────────────────────────────────────────────────────────┐
│ ① 交互层                                                            │
│    Streamlit 分析页：问题输入 → 结论 + 瀑布图 + 下钻表        ⬜      │
│    FastAPI：POST /analyze   GET /report/{id}   GET /health     ⬜      │
│    （存量可参考：streamlit_app.py 41.7KB、backend/routes 8 个模块）   │
├─────────────────────────────────────────────────────────────────────┤
│ ② 编排层                                                            │
│    主线：确定性 DAG + 2 个 LLM 节点                           ⬜      │
│      ①plan(LLM) → 取数/指标(代码) → 异常检测(统计)                    │
│      → 归因下钻(代码) → ⑥narrate(LLM) → 模板渲染(代码)               │
│    实验线：手写 ReAct 循环（不依赖框架）                      ✅      │
│      agent_lab/p1_react.py（296 行）                                │
├─────────────────────────────────────────────────────────────────────┤
│ ③ 分析层                                                            │
│    工具集 agent_lab/tools.py（367 行，4 个工具）              ✅      │
│      query_metrics / monthly_trend / rank_dimension                 │
│      contribution_breakdown（量价三因子分解 + 加总断言）             │
│    异常检测 anomaly.py（日历校正/MAD/最小支持度/变点）        ⬜      │
│    归因下钻 attribution.py（总量→平台→商品→时间）             ⬜      │
│    报告渲染 report.py + templates/（Jinja2 → MD/PDF）         ⬜      │
├─────────────────────────────────────────────────────────────────────┤
│ ④ 数据层                                                            │
│    MySQL 8.4.9（便携版）· orders 表 · 102,287 行              ✅      │
│    KPI 口径表（5 个指标，随工具返回值一并返回）                ✅      │
│    数据能力盘点（audit_data_capability.sql）                  ✅      │
├─────────────────────────────────────────────────────────────────────┤
│ ⑤ 评测层                                                            │
│    防幻觉数字对账（回答中每个数字回库复算 5/5）                🟡手工   │
│    压测体系（6 场景 + 故障注入钩子）                          ✅      │
│    异常检测 P/R/F1（注入集 ground truth）                     ⬜      │
│    评测集 25 题（单步10/多步10/开放5）+ P0 基线对比            ⬜      │
│    pytest 单测（含分解加总断言）                              ⬜      │
├─────────────────────────────────────────────────────────────────────┤
│ ⑥ 部署层                                                            │
│    Docker Desktop + WSL2 + Compose（7 服务编排）              🟡      │
│      环境已就绪、compose config 校验通过；未端到端起过               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、两条线（这是本项目最重要的设计）

| | 主线 | 实验线 `agent_lab/` |
|---|---|---|
| **目的** | 求职作品：结论可复现、可对账 | 学 Agent：把每种机制亲手实现一遍 |
| **形态** | 确定性 DAG，LLM 只在 plan / narrate | 手写 ReAct 循环（不依赖框架） |
| **为什么** | 分析类任务 90% 步骤有唯一正确答案，不该交给模型 | 先搞清框架替你做了什么，再决定用不用它 |
| **共用** | 同一套工具（`agent_lab/tools.py`）、同一个数据库、同一套评测集 | 同左 |
| **现状** | ⬜ 未开始 | ✅ 已跑通 |

> **两条线共用地基**，所以实验线的产物天然是主线的一部分，不是玩具。

---

## 三、目录结构（✅ 实际存在 / ⬜ 计划）

```
ai-commerce-intelligence-platform/
├── agent_lab/                        ← 我新增（实验线）
│   ├── __init__.py                   ✅  包说明（双线关系）
│   ├── tools.py                      ✅  4 个纯函数工具 + 口径表 + 参数白名单
│   ├── p1_react.py                   ✅  手写 ReAct：循环/停止条件/错误回传/轨迹
│   ├── p1_stress.py                  ✅  6 场景压测 + 故障注入钩子
│   ├── p0_single_call.py             ⬜  单轮调用基线（拿"循环 vs 单轮"对比数字）
│   ├── anomaly.py                    ⬜  异常检测
│   ├── attribution.py                ⬜  归因下钻
│   ├── report.py                     ⬜  报告渲染 + 数字对账
│   ├── templates/                    ⬜  Jinja2 报告模板
│   ├── evaluate.py                   ⬜  评测入口（跑全部指标）
│   └── tests/                        ⬜  单测（含分解加总断言）
│
├── analysis/                         ⬜  主线分析纯函数层（或并入 agent_lab/）
│
├── sql/
│   ├── 01_create_table.sql           ✅  存量
│   ├── 02_import_data.sql            ✅  存量（容器路径版）
│   └── 02_import_data.windows.sql    ✅  我新增（仅改路径，原文件未动）
│
├── data/cleaned_orders.csv           ✅  存量（16MB / 102,287 行）
│
├── agent_core/                       ✅  存量（Text-to-SQL 工作流，本项目的"邻居能力"）
├── backend/                          ✅  存量（FastAPI 32 接口 / JWT / Redis / 限流）
├── ai-ecommerce-assistant/           ✅  存量（RAG 助手 + 评测脚本；我修了 run_sql_eval 的 dotenv bug）
├── streamlit_app.py                  ✅  存量（BI 看板）
├── docker-compose.yml / deploy/      ✅  存量（7 服务编排）
└── README.md                         ⬜  待补我的架构图 + 评测结果表
```

---

## 四、一次完整请求的数据流（现在能跑通的部分）

以 **"11 月销售涨了，是不是靠单量堆的？"** 为例：

```
用户问题
   ↓
p1_react.run_agent()                                    ✅ 手写循环
   │
   ├─ Step 1 · LLM 决策（唯一用模型的地方之一）
   │    Thought: 需要做量价结构分解
   │    Action: contribution_breakdown
   │    Action Input: {"dimension":"platform","period_a":"2025-10","period_b":"2025-11"}
   │         ↓
   │    tools.call_tool()
   │      ① 参数白名单校验（维度枚举 / 月份 01~12 / 年份范围）   ✅
   │      ② 只读 SQL（仅 SELECT）→ MySQL 102,287 行              ✅
   │      ③ 代码计算：量效应 / 价效应 / 交互项 + 加总校验          ✅
   │      ④ 返回口径说明（definition/unit）+ reading_guide       ✅
   │         ↓
   │    Observation 回传（超 1800 字符截断）                     ✅
   │
   ├─ Step 2 · LLM 决策
   │    Thought: 数据足够
   │    Final Answer: 引用工具给的数字，不自己算                 ✅
   │
   ↓
结果 + 完整轨迹（每步 thought / action / args / observation / 耗时 / token）  ✅
   ↓
（待补）数字对账脚本 → 报告渲染 → 落库 → API/UI                ⬜
```

**关键分工**：整条链路里 **LLM 只出现在 Step 1 和 Step 2**；取数、校验、计算、加总校验全部是代码。

---

## 五、模块职责与现状

| 模块 | 职责 | 输入 → 输出 | 现状 |
|---|---|---|---|
| `tools.py` | 把业务问题翻译成只读 SQL，并保证口径统一 | 结构化参数 → 带口径的 JSON | ✅ 367 行 |
| `p1_react.py` | 多步编排：让模型决定查什么、查几次 | 自然语言问题 → 答案 + 轨迹 | ✅ 296 行 |
| `p1_stress.py` | 主动制造故障，暴露失败模式 | 6 个场景 → 失败模式清单 | ✅ 118 行 |
| `anomaly.py` | 判断"这个波动是否异常" | 时间序列 → 异常点 + 分数 + 判据 | ⬜ |
| `attribution.py` | 回答"是谁造成的、各占多少" | 两期对比 → 贡献度 + 瀑布图数据 | 🟡 分解已有，逐维下钻缺 |
| `report.py` | 渲染经营报告并**对账** | 分析结果 JSON → MD/PDF | ⬜ |
| `evaluate.py` | 跑全部指标，产出评测报告 | 评测集 → P/R/F1、对账率、成本 | ⬜ |
| FastAPI 路由 | 对外接口 | HTTP → 分析结果 | ⬜ |
| Streamlit 页 | 演示界面 | 交互 → 结论 + 图表 | ⬜ |

---

## 六、技术栈（分层 + 现状）

| 层 | 技术 | 现状 |
|---|---|---|
| 数据 | MySQL 8.4.9（便携版，无需管理员）、`LOAD DATA`、索引、只读账号 | ✅ |
| 分析 | Python 纯函数、SQL 聚合、量价三因子分解 | ✅ |
| Agent | **手写 ReAct（原生 HTTP 调 LLM）**、工具 schema、停止条件、错误回传、轨迹 | ✅ |
| LLM | DeepSeek（OpenAI 兼容协议，`deepseek-chat`） | ✅ |
| 编排 | LangGraph 确定性 DAG（计划） | ⬜ |
| 服务 | FastAPI、JWT、Redis、Nginx（存量可复用） | ⬜ |
| 前端 | Streamlit + Plotly（存量可参考） | ⬜ |
| 部署 | Docker Desktop + WSL2 + Docker Compose（7 服务） | 🟡 |
| 测试 | pytest（存量 88 项 + 我将新增） | ⬜ |
| 评测 | 数字对账、注入集 P/R、评测集回归、成本统计 | 🟡 |

---

## 七、关键设计决策（面试被问"为什么"时的答案）

1. **为什么不让 LLM 自己算数？**
   → 代码算的结果可复现、可对账；模型的职责是"理解问题"和"表达结论"。实测：回答里 5/5 数字全部可回库复算，零幻觉。

2. **为什么工具返回值里要带口径说明？**
   → 避免"同一个问题两次问出两个数"。口径随值一起返回，模型直接引用，不用自己推断字段含义。

3. **为什么量价分解必须是三因子而不是两因子？**
   → `ΔR = 量效应 + 价效应 + 交互项`，三项**精确加总回总变化**才能断言校验（实测闭合 0.0）。少一项就变成近似，无法验证。

4. **为什么工具要只读 + 参数白名单？**
   → Agent 安全第一层。参数校验不到位时，模型会替工具"擦屁股"（实测：`2025-13` 被放过时，模型只能自己推断"13 月不存在"）——**错误信息越明确，模型自我修正越可靠**。

5. **为什么手写 ReAct 而不是直接上框架？**
   → 先知道自己实现时踩什么坑（重复动作检测误伤重试、上下文膨胀、畸形输出），才能在框架上做出正确的选型判断。

6. **为什么不硬塞 RAG？**
   → 结构化数据分析的正确工具是 SQL 和代码，不是向量检索。RAG 在本项目只保留一个用途：**口径说明与政策依据的引用**。

7. **为什么砍掉区域/销售员维度？**
   → 数据里根本没有这两个字段（能力盘点实测）。不做假装有数据的分析。

---

## 八、与存量系统的边界（面试必答："哪部分是你写的"）

| 类别 | 内容 |
|---|---|
| **我新增** | `agent_lab/` 全部 4 个文件（786 行）；`sql/02_import_data.windows.sql`；本机 MySQL 8.4.9 便携版部署与 102,287 行导入；`.env` 配置（LLM Key + DB 凭据 + compose 变量） |
| **我修改** | `ai-ecommerce-assistant/eval/run_sql_eval.py`（修 `.env` 加载顺序 bug：先读 `os.environ` 后才 `load_dotenv`，导致 CLI 裸环境必报"Key 未配置"） |
| **存量复用（不是我写的）** | `agent_core/*`（Text-to-SQL 工作流）、`backend/*`（32 接口）、`ai-ecommerce-assistant/*`（RAG）、`streamlit_app.py`、`docker-compose.yml`、`deploy/*`、`sql/01-03`、`data/cleaned_orders.csv` |

> 面试话术："我是在一个已跑通的开源企业级项目上做增量演进：它已经有完整的数据、接口、部署和评测体系，但**只有单轮问数，没有'发现异常→下钻→归因→报告'的分析闭环**。我补的是分析内核和 Agent 机制这两块。"

---

## 九、缺口清单（按优先级，下一步做什么）

| 优先级 | 任务 | 难度 | 预估 | 产出 |
|---|---|---|---|---|
| **P0** | 注入集（12 个已知异常）+ 异常检测 + P/R/F1 | 中 | 1 天 | **简历第三个硬指标** |
| **P0** | 报告生成（Jinja2 → MD/PDF）+ 自动数字对账 | 低 | 半天 | 交付物 + 对账通过率 |
| P1 | pytest 单测（含分解加总断言） | 低 | 2 小时 | CI 可跑 |
| P1 | FastAPI 3 接口 | 低 | 半天 | Swagger 可演示 |
| P1 | Streamlit 一页（结论 + 瀑布图 + 下钻表） | 低 | 半天 | 可录屏演示 |
| P2 | Compose 端到端跑通 | 低（可能卡网络） | 半天~1天 | 一键起 |
| P2 | P0 单轮基线 + 25 题评测集对比 | 中 | 半天 | "循环 vs 单轮"数据 |
| P2 | README（架构图 + 评测结果表） | 低 | 2 小时 | 仓库门面 |
| P3 | HITL 审批 / P4 Supervisor 多 Agent / 行级权限 | 高 | — | 面试谈资，非必需 |

# BOSS 直聘 · 在线简历逐字段填空稿

> 用法：BOSS 网页版打开「我的简历 / 在线简历」→ 按下面顺序逐栏复制粘贴。
> **为什么在线简历比 PDF 更重要**：很多 HR 只扫在线简历、不点附件；而且 BOSS 的搜索/推荐是**按在线简历的关键词**把你推给 HR 的。
> ⚠️ 手机 / 邮箱两栏请照 PDF 简历填（本文不写个人信息）。

---

## 一、基本信息

| 字段 | 填什么 |
|---|---|
| 姓名 | 钟英浩 |
| 性别 | 男 |
| 出生年月 | 2003.04 |
| 现居地 | 广东省东莞市 |
| 学历 | 本科 |
| 毕业院校 | 桂林理工大学 |
| 专业 | 遥感科学与技术 |
| 毕业时间 | 2026.06 |
| **工作年限** | **应届生**（若平台无此选项选「1 年以内」，备注写「26 届毕业生，择业期内」） |
| **求职状态** | **离职-随时到岗** ← 最重要，HR 常用这个筛人 |

---

## 二、求职意向

| 字段 | 填什么 | 说明 |
|---|---|---|
| **期望职位** | 主选 **AI应用开发**；备选 **Python开发** | 这一栏决定你被推进哪个岗位池，选大词覆盖广 |
| 期望行业 | 人工智能 / 互联网 / 计算机软件 | 也可以选「不限」，覆盖更广 |
| 期望城市 | **深圳** | 你在东莞，建议备注或自我介绍里强调"随时可到深圳到岗" |
| 期望薪资 | **面议**（或 8–12K） | 写太高会被过滤，太低显得不自信；不确定就用「面议」 |
| 到岗时间 | **随时到岗** | |
| 工作性质 | 全职 | |

> **小技巧**：BOSS 的「期望职位」是下拉选，如果搜不到"AI应用开发"，就选 **"Python开发工程师"** 或 **"算法工程师"**，再在个人优势里把 AI 关键词写足。

---

## 三、个人优势（这一栏 HR 第一眼看，要**关键词密集 + 可量化**）

直接复制（约 600 字）：

```
【Agent 开发】手写 ReAct / Function Calling 循环（不依赖框架），
实现多轮工具编排、停止条件与 token 预算、重复动作检测、错误回传自校验；
再用 LangChain / LangGraph create_agent 落地多工具 Agent。熟悉 Prompt 工程与工具描述路由。

【RAG 全链路】文档分块 → Embedding → Chroma 向量检索 → 生成；
自建 12 题评测集完成 chunk_size×k 四组对比，检索命中率 83%→100%；熟悉防幻觉数字对账。

【数据分析与评测】量价三因子分解（可精确加总，断言校验闭合 0.0）；
异常检测（周内效应校正 + MAD 稳健统计 + 故障注入集 P/R 评测，查全 100%、无关误报 0）；
经营报告自动生成 + 两道数字对账（文本级 0 违规 / 数据库级复算全一致）。

【工程化】Python、MySQL、SQLite、FastAPI、Streamlit、Docker、Docker Compose、Nginx、
Redis、Linux、Git、pytest；能独立完成从数据到上线到评测的完整链路。

【AI 辅助开发】熟练使用 opencode CLI + DeepSeek 等 Code Agent 做代码生成、Review、重构与排障。

【专业背景】遥感科学与技术本科，具备空间数据分析与定量归因能力
（毕设：云贵高原 10 大湖泊 2013–2022 面积时序分析，6 种水体指数精度对比 + K-Means + 回归归因）。
```

> **关键词清单**（BOSS 搜索靠这些词命中，确保简历里都出现过）：
> Agent、大模型、LLM、RAG、LangChain、LangGraph、Function Calling、Prompt、向量数据库、Chroma、
> Embedding、FastAPI、Python、MySQL、Docker、评测、数据对账

---

## 四、项目经历（BOSS 有独立栏目，加两条）

### 项目一

| 字段 | 填什么 |
|---|---|
| 项目名称 | 销售经营分析 Agent（真实电商数据集） |
| 角色 | 独立开发 |
| 起止时间 | 2026.08 – 至今 |

**项目描述**（复制）：

```
在开源企业级项目（FastAPI + MySQL + Redis + Nginx + LangGraph）基础上做增量演进，
补齐其缺失的「发现异常 → 逐维下钻 → 量价归因 → 经营报告」分析闭环。
数据为 102,287 行真实电商订单，全部结论可回数据库对账。
```

**我的职责 / 业绩**（复制）：

```
1. 数据层：用 LOAD DATA 将 10.2 万行订单 2.3 秒导入 MySQL；建立 5 个 KPI 口径表，
   口径随工具返回值一并返回，避免"同一问题两次问出两个数"。
2. 归因内核：实现量价三因子分解（量效应 / 价效应 / 交互项），三项精确加总回总变化
   （断言校验闭合 0.0）。真实结论：2025-10→11 销售额 +15.92%，量效应 +202.07 万、
   价效应 −51.26 万，属于典型"以量补价"。
3. Agent 层：不依赖框架手写 ReAct 循环，实现步数上限 / token 预算 / 重复动作检测 /
   observation 截断 / 错误回传 / 每步轨迹与 token 统计；工具只读且经参数白名单校验。
4. 异常检测：周内效应校正（同星期几基线）+ 日历校正 + MAD 稳健统计 + 最小支持度门槛；
   用故障注入构造带 ground truth 的评测集，差分口径评测查全 100%（6/6）、无关误报 0。
5. 报告与对账：Jinja2 模板生成经营报告，数字全部由代码计算；发布前两道对账
   （文本级 88 个数字 0 违规、数据库级复算 11/11 一致），并对对账器本身做注入自测。
6. 工程化：FastAPI 6 个接口（含边界校验 422 / 资源不存在 404）+ Streamlit 看板
   （量价瀑布图 / 维度下钻 / 异常发现 / 报告对账 / 自然语言问数）。
```

### 项目二

| 字段 | 填什么 |
|---|---|
| 项目名称 | 遥感监测智能助手（RAG + 多工具 Agent） |
| 角色 | 独立开发 |
| 起止时间 | 2026.07 – 2026.09 |

**项目描述**（复制）：

```
基于 RAG + 多工具 Agent 的遥感地质灾害智能问答系统：支持自然语言查询地面沉降、
滑坡预警、形变风险等问题，输出带法规依据的结构化报告与免责声明。
Docker 部署于阿里云 ECS，公网可访问。
```

**我的职责 / 业绩**（复制）：

```
1. RAG：用 gov.cn 真实法规《地质灾害防治条例》构建知识库（切分 → text-embedding-v3 →
   Chroma），先在 day7-10 手写全链路，后用 LangChain 重构。
2. Agent：用 LangGraph create_agent 集成三个工具——法规向量检索 / SQLite 真实数据集查询 /
   形变风险规则打分；风险评估用确定性 Python 规则（速率+加速+异常三指标），不交 LLM 评定。
3. 评测：自建 12 题法规问答测试集，完成 chunk_size(100/400) × k(2/4) 四组对比实验，
   检索命中率从 83% 提升到 100%；并分析了 k 的"效果 vs 成本"权衡。
4. 工程化：FastAPI + Streamlit 双入口；Docker 部署阿里云 ECS 公网可访问；
   API Key 环境变量化；跨设备 Git 协作。
```

---

## 五、教育经历

| 字段 | 填什么 |
|---|---|
| 时间 | 2022.09 – 2026.06 |
| 学校 | 桂林理工大学 |
| 专业 | 遥感科学与技术 |
| 学历 | 本科 |
| 主修课程 | 测量学、GIS、遥感概论、空间分析、摄影测量、误差分析、低空无人机遥感、模式识别与机器学习 |

---

## 六、技能标签（BOSS 会让你选/填，尽量全选）

```
Python、LangChain、LangGraph、RAG、Agent、大模型、LLM、Prompt Engineering、
向量数据库、Chroma、FastAPI、MySQL、SQL、Docker、Git、Streamlit、Linux、pytest
```

> 标签越全，被 HR 搜到的概率越高。**别嫌多**。

---

## 七、附件简历

上传 **AI 版 PDF**（`钟英浩_AI应用开发_简历_深圳版` 导出的 PDF）。
投 GIS/内业岗时再换成 GIS 版，**两版别混投**。

---

## 八、填完后的三个自检

- [ ] **简历完善度**到 100%（BOSS 会显示，没满就继续填）
- [ ] 打开「**竞争力分析**」，看平台给你的建议并照改
- [ ] 用 HR 视角自查一遍：**第一屏能不能看到"Agent / RAG / Python + 深圳 + 随时到岗"**？看不到就把关键词往前提

---

## 九、诚实红线（面试会被追问，别写虚的）

- ❌ 销售分析项目**没有公网部署**（API/看板都在本机）→ 写"可复现"或"GitHub 可查"，**别写"已上线"**
- ✅ 遥感监测助手**确实公网可访问** → 可以写"已上线"，附链接
- ❌ 不要写"精通"；写"熟练 / 熟悉"并给证据
- ✅ 没写进去的东西，面试时主动说"这块我在做/还没深入"，比被追问出来强

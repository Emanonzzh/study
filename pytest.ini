[pytest]
# 只收集真正的单元测试目录。
# 为什么必须显式限定：pytest 默认会收集 *_test.py，
# 而 agent_lab/api_smoke_test.py 和 ui_smoke_test.py **需要先启动服务和看板**，
# 放开收集会让 `pytest` 在干净环境下必然失败。
testpaths = agent_lab/tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    slow: 较慢但仍是纯逻辑的测试

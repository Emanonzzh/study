# 项目 V2 升级任务拆解

> 项目已部署上线（http://47.76.101.97:8501）。V2 目标：从"问答+查询"升级为"智能分析"，提升项目深度与简历含金量。
> 时间预算：约 1 周，**边投简历边做**（先投后升，每阶段完成更新简历）。

## 核心原则（重要）

1. **不动架构**：保持 `create_agent` + 多工具不变，V2 只加工具和数据，不重构 LangGraph 状态图、不做显式 Query Router（LLM 自主决策本身就是路由）
2. **风险评估用 Python 规则计算，不交给 LLM 瞎评**（LLM 只负责把结果解释成人话）
3. **数据诚实原则**：示例数据标注"示例/演示数据"；真实数据必须可引用出处
4. 每阶段收尾：git push + 更新 AGENTS.md + 更新简历一行 + 重新部署（docker build/run）

---

## 阶段 A：风险评估工具（1-2 天）⭐ 优先

**目标**：Agent 增加第三个工具 `risk_assessment`，能回答"某区域形变风险如何"。

**任务拆解**：

1. **建时间序列表**（day22 或直接改 day19 建库脚本）
   - 新表 `monitoring_series`：`id, region, date, deformation_mm, source`
   - 插入示例数据：某区域 2024-01 至 2025-06 月度形变量（递增趋势，**注释标注"示例演示数据"**）

2. **写 `risk_assessment` 工具**（规则打分，不用 LLM）
   - 参数：`region`
   - 内部逻辑：查时间序列 → 算三个指标：
     - 形变速率（mm/月）超阈值 → +40 分
     - 最近阶段加速（后期斜率 > 前期斜率）→ +30 分
     - 异常检测（最近一期变化率 > 历史平均 3 倍）→ +30 分
   - 分数分级：`0-30 低风险` / `30-60 中风险` / `60-100 较高风险`
   - 返回：分数 + 等级 + 三个指标明细（字符串）

3. **更新 day21**：`create_agent(llm, [query_regulation, query_dataset, risk_assessment])`

4. **结构化报告 + 免责声明**：让 LLM 按"数据概况/形变分析/风险等级/法规依据/结论"格式输出，末尾必须带：
   > "本系统结果仅基于公开数据辅助分析，不构成专业灾害预测或官方预警。"

5. **验收**：问"分析 XX 区域的形变风险"→ Agent 调 risk_assessment → 输出风险等级 + 完整报告

---

## 阶段 B：数据分析工具（2-3 天）

**目标**：Agent 增加第四个工具 `deformation_analysis`，能输出统计 + 趋势 + 异常。

**任务拆解**：

1. **统计**：max / min / mean / 累计形变 / 平均速率（手写循环，对应 practice_day1 题 1 的逻辑，不许用内置 max/min/sum——练基础）
2. **趋势**：首尾差值 + 最近 3 期斜率（简单版，不上线性回归）
3. **异常**：最近一期变化率 vs 历史平均变化率，超 3 倍判异常
4. **更新 create_agent** 加入第四个工具
5. **验收**：问"分析 XX 区域形变趋势"→ 返回统计 + 趋势判断 + 异常提示

---

## 阶段 C：RAG 评估（1-2 天）

**目标**：用测试集对比不同切片/检索参数，优化 RAG 并写出实验结论。

**任务拆解**：

1. **建测试集**：10-15 个法规问答对（问题 + 期望命中的条款号），如"地质灾害分为几等级→第四条"
2. **写评估脚本** `rag_eval.py`：对每个问题做检索，判断期望条款是否出现在 Top-K 结果中
3. **对比实验**：chunk_size 100 vs 400 × k=2 vs 4，共 4 组，输出命中率对比表
4. **选最优参数**更新 day21/web_rag
5. **简历加一句**："构建法规问答测试集，对切片策略与检索参数进行对比实验，优化检索命中率"

---

## 每阶段收尾动作（固定流程）

```
① git add + commit + push（新工具/新文件）
② 更新 AGENTS.md 学习进度
③ 简历对应加一行
④ 服务器重新部署：git pull → docker build -t rs-assistant . → docker rm -f rs-app → docker run ...
⑤ 公网验收（http://47.76.101.97:8501）
```

---

## 明天白天（另一台电脑）具体任务

**阶段 A 第 1、2 步**：

1. 设计并建 `monitoring_series` 表，插入示例时间序列数据（诚实标注示例）
2. 写 `risk_assessment` 工具（规则打分，参考 check_alarm 思路的扩展）

教学要求：给任务和思路，让学习者自己写；工具内 SQL 查询和字符串拼接他都会。

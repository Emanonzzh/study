# Agent 面试题扫盲清单

> 来源：一组高级 Agent 工程师面试题（共 12 道）。应届生岗不会全考，但概念扫盲后能答上大部分。
> 状态：✅ 已会 / ⚠️ 半会 / 📖 扫盲级（半天补完）/ 🔮 秋招后学

## 分级总览

| 题号 | 问题 | 状态 |
|---|---|---|
| 8 | 工具注册机制如何实现 | ✅ 已会 |
| 5 | 功能无法实现如何处理 | ⚠️ 半会 |
| 1 | Agent 最大轮数 | 📖 |
| 2 | 上下文长度控制 | 📖 |
| 3 | 失败退出策略 | 📖 |
| 4 | 记忆权衡 | 📖 |
| 6 | 可观测体系 | 📖 |
| 7 | 每轮日志留痕 | 📖 |
| 9 | 参数校验、权限划分 | 📖 |
| 10-12 | MCP 三题 | 🔮 秋招后学（2 天入门）|

## 逐题答案要点（扫盲参考）

**1. Agent 最大轮数**
`create_agent` 的 `max_iterations` 参数控制（默认约 25）。作用：防止 Agent 无限循环调工具烧 token，到达上限强制停止。

**2. 上下文长度控制**
滑动窗口（截断旧消息）、关键信息摘要压缩、控制工具返回内容长度、检索结果按需裁剪（k 值）。

**3. 失败退出策略**
try/except 捕获异常并返回错误信息；设置单次调用超时；max_iterations 兜底强制退出；工具失败时如实返回让 LLM 决定后续。

**4. 记忆权衡**
短期记忆=消息窗口；长期记忆=向量库/数据库；记忆过多→超 token 上限+噪音干扰；过少→丢上下文。做法：分层存储 + 关键信息摘要。

**5. 功能无法实现**
工具如实返回"未找到/无法处理"，LLM 诚实告知用户并给替代建议，**绝不编造**（即自己项目 prompt 里"材料里没有就说不知道"的原则）。

**6. 可观测体系**
日志（每轮工具调用）+ 追踪平台（LangSmith/Langfuse）+ 指标（响应延迟、工具成功率、token 花费）+ 告警。

**7. 每轮日志留痕**
每条记录：时间戳、轮次、工具名、入参、出参、耗时、token 数。留痕目的：排查"Agent 哪一步走错"。

**8. 工具注册机制（已会）**
`@tool` 装饰器把函数注册为工具（schema 自动从 docstring + 类型注解生成），`create_agent(llm, [工具列表])` 注入 Agent。

**9. 参数校验、权限划分**
校验：工具函数内部检查参数（必填/类型/范围），非法参数返回错误信息。权限：不同 Agent 配不同工具列表；敏感工具加鉴权层。

**10-12. MCP（秋招后单独学）**
MCP = 模型上下文协议，把工具做成标准 server，多个 Agent 作为 client 共用。三题核心：只暴露必要工具避免上下文污染；协议天然支持多 Agent 连接同一 server；初级项目"套壳"不可耻，但要能讲出手写过底层 Function Calling 循环（理解机制）。

## 行动

1. 明天扫盲 1-7、9 题（每题 10-20 分钟），整理进面试八股笔记
2. MCP 单独排期：秋招后 2 天入门
3. 面试被问到时，能答概念 + 结合自己的 day21 项目举例
